<form method="post" action="{{ path('app_calandar_day_delete', {'id': calandar_day.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ calandar_day.id) }}">
    <button class="btn">Delete</button>
</form>
 