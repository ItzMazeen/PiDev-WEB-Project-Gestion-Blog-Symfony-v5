<form method="post" action="{{ path('app_events_ticket_delete', {'id': event_ticket.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event_ticket.id) }}">
    <button class="btn btn-primary py-2 px-4 mt-4">Supprimer</button>
</form>
