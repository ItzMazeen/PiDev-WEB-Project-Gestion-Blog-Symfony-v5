<form method="post" action="{{ path('app_fiche_delete', {'id': ficheconsultation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ficheconsultation.id) }}">
   <center> <button class="btn btn-primary w-50 m-2">Delete</button></center>
    
</form>
