{% extends 'baseback_dossier.html.twig' %} 
{% block title %}Ficheconsultation{% endblock %}



{% block body %}
<div class="bg-transparant rounded h-50 p-3">
    <h1>Ficheconsultation</h1>

    
     
<div class="container">
  <div class="row">
    <div class="col-lg-4 mx-auto my-3 wow slideInUp" data-wow-delay="0.3s">
      <div class="bg-white rounded shadow position-relative" style="z-index: 1;">
        <div class="border-bottom py-4 px-5 mb-4">
            <h4 class="text-primary mb-1">{{ ficheconsultation.firstName }} {{ ficheconsultation.lastName }}</h4>
            <small class="text-uppercase">{{ ficheconsultation.dateConsultation ? ficheconsultation.dateConsultation|date('Y-m-d') : '' }}</small>
        </div>
            </h1>
            <div class="d-flex  mb-3"><span class="text-center">specialite:</span><span>{{ ficheconsultation.specialite }}</span></div>
            <div class="d-flex  mb-3"><span>traitement:</span><span>{{ ficheconsultation.traitement }}</span></div>
            <div class="d-flex  mb-3"><span>reccomendation:</span><span>{{ ficheconsultation.reccomendation }}</span></div>
     </div>
    </div>
 </div>
</div>
<center>
    <a href="{{ path('app_fiche_index') }}"><button type="button" class="btn btn-primary w-50 m-2">back to list</button></a>

    <a href="{{ path('app_fiche_edit', {'id': ficheconsultation.id}) }}"><button type="button" class="btn btn-primary w-50 m-2">edit</button></a>
</center>
    {{ include('fiche/_delete_form.html.twig') }}
{% endblock %}
