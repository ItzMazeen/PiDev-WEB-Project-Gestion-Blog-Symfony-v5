<form method="post" action="{{ path('app_commentaire_delete1', {'id': commentaire.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commentaire.id) }}">
    <button class="btn btn-outline-primary">Supprimer</button>
</form>
